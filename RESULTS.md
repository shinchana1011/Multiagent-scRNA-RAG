# Results — Member 1 Pipeline Validation

Numbers generated by `python -m scripts.validate_pipeline1`, recorded here as they land.

## Harmony batch correction (NFR-07)

Dataset: COVID GSE145926 (BALF, 11 patients), real data.

| Metric | Value | Target | Result |
|---|---|---|---|
| Cells (raw → post-QC) | 101,981 → 58,743 | — | 43% removed (expected — BALF has more dead/dying cells than clean PBMC) |
| Patients | 11 | — | |
| Leiden clusters | 12 | — | |
| iLISI | 2.21 | > 1.5 | **PASS** |

Visual check: `data/processed/figures/umap_patient.png` — patient colors are mixed
throughout most clusters. One cluster (bottom-middle) is still dominated by
patients C51/C52; worth investigating as a possible patient-specific cell
state before assuming it's a mixing failure (overall iLISI still passes).

## Scalability (NFR-02)

100k-cell dataset (the COVID run above, 101,981 cells) completed end-to-end
(QC → markers) in ~5.5 minutes with no memory failure. Target: 100k cells
without failure — **met**.

## ARI clustering benchmark (NFR-04)

| Dataset | Ground-truth types | Leiden clusters | ARI | Target | Result |
|---|---|---|---|---|---|
| Synthetic 3-cluster (plumbing test, `data/raw/zheng68k/synthetic_labeled.h5ad`) | 3 | 3 | 1.000 | ≥ 0.75 | code path verified, not a real benchmark |
| Zheng68k (real, labeled) | — | — | — | ≥ 0.75 | **pending** — needs labeled Zheng68k download |

The synthetic result only proves `benchmark_ari()` computes and reports
correctly (cleanly-separated synthetic clusters recover perfectly, as
expected). The real headline ARI number requires downloading the labeled
Zheng68k dataset into `data/raw/zheng68k/zheng68k.h5ad` and re-running
`python -m scripts.validate_pipeline1`.

## Open items

- [ ] Download labeled Zheng68k, get the real ARI number
- [ ] Investigate the C51/C52-dominated cluster in the COVID UMAP
